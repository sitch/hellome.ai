/* eslint-disable */
import { z } from "zod"

export const PageArtworkScalarFieldEnumSchema = z.enum([
  "status",
  "pageId",
  "storyId",
  "artistId",
  "photoId",
  "id",
  "createdAt",
  "updatedAt",
  "deletedAt",
])
